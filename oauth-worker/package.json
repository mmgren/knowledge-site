{
  "name": "knowledge-site-oauth-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test src/index.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
